# 음수 인덱스를 활용하면 뒤에서부터 직접 접근할 수 있습니다.
scores = [85, 92, 78, 95, 88, 70, 100, 65, 82, 90]
k = int(input())

# 알고리즘: scores[-k] → 끝에서 k번째 원소에 O(1) / O(1) 접근
# 입력값 범위 검증: k가 유효 범위(1~len(scores))를 벗어나면 안내 메시지 출력
if 1 <= k <= len(scores):
    print(scores[-k])